const mode = process.argv[2] || 'start';
const port = process.env.PORT || 3002;

console.log(`[mfe-nasdaq] ${mode} server ready on port ${port}`);
console.log('[mfe-nasdaq] Replace this placeholder with your application entry point.');
