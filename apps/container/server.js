const mode = process.argv[2] || 'start';
const port = process.env.PORT || 3000;

console.log(`[container] ${mode} server ready on port ${port}`);
console.log('[container] Replace this placeholder with your application entry point.');
